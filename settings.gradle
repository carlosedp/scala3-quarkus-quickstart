pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.9.4"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
