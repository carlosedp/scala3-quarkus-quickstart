pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.13.2"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
