pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.17.7"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
