pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.16.2"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
