pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.10.1"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
