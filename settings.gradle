pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.22.3"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
