pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "io.quarkus" version "3.25.0"
    }
}

rootProject.name = 'scala3-quarkus-quickstart'
